// Since you are not using authentication for this project, we can remove all auth-related code.
// If you decide to add authentication later, you can reintroduce this functionality.

console.log("Authentication is disabled for this project.");

// You can still set up UI elements related to temperature and humidity here if needed.
function setupUI(user) {
    // Implement any UI updates related to your app, if required.
    // For instance, showing/hiding elements based on user authentication status.
}

// Example: Setting up the UI for temperature and humidity monitoring
function setupMonitoringUI() {
    // Set up your UI elements that display temperature and humidity readings here
    console.log("Setting up monitoring UI.");
}

// Call this function to initialize the UI when the page loads
setupMonitoringUI();